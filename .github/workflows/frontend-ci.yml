name: Frontend CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'frontend/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js 18.x
      uses: actions/setup-node@v3
      with:
        node-version: 18.x
        cache: 'npm'
        cache-dependency-path: './frontend/package-lock.json'

    - name: Install dependencies
      run: npm ci

    - name: Build React App
      run: npm run build
      env:
        CI: true

    - name: Run ESLint
      run: npx eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0
      continue-on-error: true # Warning only initially

    # If vitest tests are added later, uncomment this step
    # - name: Run Vitest
    #   run: npm run test
